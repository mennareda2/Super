<!DOCTYPE html>
<html lang="ar">
    <%- include('../partials/header') %>
<body>
    <%- include('../partials/navbar') %>

    <div class="container">
        <h1>جميع المنتجات المتوفرة (Total: <%= products.length %>)</h1>
        <hr>

        <% if (products.length === 0) { %>
            <div class="alert alert-info">
                لا توجد منتجات مسجلة حالياً. <a href="/products/new">أضف منتجك الأول!</a>
            </div>
        <% } %>

        <div class="row">
            <% for(let product of products) { %>
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm h-100">
                    <div class="card-body">
                        <h5 class="card-title text-primary"><%= product.name %></h5>
                        <p class="card-text text-success">
                            **السعر:** $<%= product.price.toFixed(2) %>
                        </p>
                        <p class="card-text">
                            **المخزون:** <%= product.stockQuantity %> وحدة
                        </p>
                        <a href="/products/<%= product._id %>" class="btn btn-info btn-sm">عرض التفاصيل</a>
                    </div>
                </div>
            </div>
            <% } %>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>